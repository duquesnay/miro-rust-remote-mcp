# DEPRECATED: This file format is no longer used
# The application now reads configuration from:
# ~/.config/mcp/miro-mcp-server/config.json
#
# See config.example.json for the new JSON-based configuration format
#
# Setup instructions:
# 1. mkdir -p ~/.config/mcp/miro-mcp-server
# 2. cp config.example.json ~/.config/mcp/miro-mcp-server/config.json
# 3. Edit the config.json file with your Miro OAuth2 credentials
#
# Legacy .env format below (for reference only):
#
# MIRO_CLIENT_ID=your_client_id_here
# MIRO_CLIENT_SECRET=your_client_secret_here
# MIRO_REDIRECT_URI=http://localhost:3010/oauth/callback
# TOKEN_ENCRYPTION_KEY=generate_with_openssl_rand_hex_32
# MCP_SERVER_PORT=3010
