# Miro OAuth2 Configuration
# Obtain these from: https://developers.miro.com/ → Your Apps → Create new app
MIRO_CLIENT_ID=your_client_id_here
MIRO_CLIENT_SECRET=your_client_secret_here

# OAuth2 Redirect URI (must match Miro app configuration)
# IMPORTANT: Register this exact URL in Miro Developer Portal under "Redirect URI for OAuth2.0"
# For local development, use: http://localhost:3010/oauth/callback
# For production, use: https://your-domain.com/oauth/callback
MIRO_REDIRECT_URI=http://localhost:3010/oauth/callback

# Token Encryption Key (generate with: openssl rand -hex 32)
# SECURITY: Keep this secret! Generate a new one with: openssl rand -hex 32
TOKEN_ENCRYPTION_KEY=generate_with_openssl_rand_hex_32

# MCP Server Port
MCP_SERVER_PORT=3010
