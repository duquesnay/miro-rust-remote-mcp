# Scaleway Container Deployment Configuration
# This file is used when deploying to Scaleway with: ./scripts/deploy.sh --env=production

# Deployment target
DEPLOY_TARGET=scaleway

# Project configuration
PROJECT_NAME=miro-mcp-server
REGION=fr-par

# Container Registry (MUST be configured with your Scaleway registry)
# Example: rg.fr-par.scw.cloud/miro-mcp
REGISTRY=rg.fr-par.scw.cloud/miro-mcp

# Container namespace (extracted from REGISTRY if not set)
# NAMESPACE=

# Docker image tag
TAG=latest

# Container resource limits
MIN_SCALE=1
MAX_SCALE=1
MEMORY_LIMIT=256
CPU_LIMIT=250

# Application environment variables
# Non-sensitive (can be in plaintext):
MIRO_CLIENT_ID=3458764647516852398
MIRO_REDIRECT_URI=https://miro-mcp-flyagileapipx8njvei-miro-mcp.functions.fnc.fr-par.scw.cloud/oauth/callback

# Sensitive secrets (MUST be in Scaleway Secret Manager):
# - MIRO_CLIENT_SECRET (managed via Secret Manager)
# - MIRO_ENCRYPTION_KEY (managed via Secret Manager)
