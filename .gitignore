# Rust
/target/
**/*.rs.bk
*.pdb

# Cargo
# Note: Cargo.lock is committed for binary projects (applications) to ensure reproducible builds
# It should only be ignored for library projects

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Environment
# Local development config (contains actual credentials - never commit)
.env
.env.local

# Environment-specific local overrides (never commit)
.env.*.local

# Template files are tracked in git:
# .env.production (Scaleway template - safe to commit)

# Secrets (CRITICAL - never commit)
secrets/
*.key
*.pem
*.secret

# Claude
.claude/settings.local.json
.claude/projects/

# Logs
*.log
logs/

# Test artifacts
/test-results/
/coverage/

# Build artifacts
*.so
*.dylib
*.dll
*.exe

# Temporary files
tmp/
temp/
*.bak
*.backup
spikes/
*.tgz
convo-archive.*
